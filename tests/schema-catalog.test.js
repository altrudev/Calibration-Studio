"use strict";
const test=require("node:test");const assert=require("node:assert/strict");const fs=require("node:fs");const path=require("node:path");
test("public schema catalog is valid JSON with unique identifiers",()=>{const dir=path.resolve(__dirname,"..","schemas"),files=fs.readdirSync(dir).filter(x=>x.endsWith(".schema.json")).sort();assert.ok(files.length>=20,`expected active schema catalog, found ${files.length}`);const ids=new Set();for(const file of files){const value=JSON.parse(fs.readFileSync(path.join(dir,file),"utf8"));assert.equal(value.$schema,"https://json-schema.org/draft/2020-12/schema",`${file}: draft`);assert.equal(typeof value.$id,"string",`${file}: id`);assert.ok(!ids.has(value.$id),`${file}: duplicate $id`);ids.add(value.$id);assert.equal(value.type,"object",`${file}: root type`);}});
